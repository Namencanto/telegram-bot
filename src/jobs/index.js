import cron from 'node-cron'
import { checkDeposits } from './checkDeposits.js' 

export function setupCronJobs(bot) {
  // console.log('Running checkDeposits job...');
  // checkDeposits(bot);
  // cron.schedule('* * * * *', () => {
    // checkDeposits(bot);
  // });
}

